import Vue from "vue";
import Vuex from "vuex";

import { AppModule } from "../AppModule";

Vue.use(Vuex);

export default new Vuex.Store({
    state: {},
    mutations: {},
    actions: {},
    modules: { AppModule }
});
